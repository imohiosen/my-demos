import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    // Configure allowed domains for external images
    domains: ['dynamic.heygen.ai'],
  },

};

export default nextConfig;
